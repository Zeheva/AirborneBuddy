@model IEnumerable<AirborneBuddy.Models.AirborneOperation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DropZone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AircraftPlatform)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PayPeriodCovered)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.JumpType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DropZone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AircraftPlatform)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PayPeriodCovered)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JumpType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
